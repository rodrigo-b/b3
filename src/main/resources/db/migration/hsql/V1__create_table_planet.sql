CREATE SEQUENCE planet_id_seq AS BIGINT START WITH 1 INCREMENT BY 1;
CREATE TABLE PLANET(
    ID INTEGER GENERATED BY DEFAULT AS SEQUENCE planet_id_seq PRIMARY KEY,
    NAME    VARCHAR(30) NOT NULL,
    TERRAIN VARCHAR(30),
    CLIMATE   VARCHAR(30), 
    TIMES_IN_MOVIE SMALLINT,
);